# ==========================================
# Spring Profile Configuration
# Uses 'local' profile by default.
# Production (Render) overrides this with:
# SPRING_PROFILES_ACTIVE=prod
# ==========================================

spring.application.name=lms
spring.profiles.active=${SPRING_PROFILES_ACTIVE:local}